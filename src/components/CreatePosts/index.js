import React, { memo } from "react";

const ManagedCreatePost = memo((props) => {
    return (
        <div className="row">
            <h1>CREATE POSTS</h1>
        </div>
    );
});

export default ManagedCreatePost;
