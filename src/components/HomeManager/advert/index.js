import React, { memo } from "react";

const ManagedAdvertComponent = memo((props) => {
    return (
     <div>
         view all advertisement here....
     </div>
    );
});

export default  ManagedAdvertComponent;